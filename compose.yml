version: '3.9'

services:
  fsapi:
    image: salmonllama/fashionscape-api:latest
    restart: always
    environment:
      DB_URI:
      DB_PORT:
      DB_USER:
      DB_PASS:
      DB_DATA:
    ports:
      - "8888:8888"
    container_name: fashionscape-api

